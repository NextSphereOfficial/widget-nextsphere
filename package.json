{
  "name": "apps",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:api": "npm --prefix ./api run dev",
    "dev:widget": "npm --prefix ./widget run dev",
    "dev": "concurrently -k -n API,WIDGET -c blue,green \"npm:dev:api\" \"npm:dev:widget\""
  },
  "devDependencies": {
    "@types/node": "^20.19.23",
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^7.6.0"
  }
}
